# ============================================
# TontineClub Backend — Dependances reelles
# Nettoye le 16/07/2026 (retrait du bloat Emergent :
# emergentintegrations, openai, google-generativeai, litellm,
# stripe, resend, boto3, pandas, numpy, tiktoken, python-jose, etc.
# aucune de ces libs n'etait utilisee dans le code)
# ============================================

# Framework API
fastapi==0.110.1
starlette==0.37.2
uvicorn==0.25.0

# Base de donnees (MongoDB async)
motor==3.3.1
pymongo==4.5.0

# Validation de donnees
pydantic==2.12.5
email-validator==2.3.0

# Authentification / securite
passlib==1.7.4
bcrypt==4.1.3
PyJWT==2.11.0

# Config
python-dotenv==1.2.1

# Dev / tests (optionnel, pas necessaire en production)
pytest==9.0.2
requests==2.32.5
